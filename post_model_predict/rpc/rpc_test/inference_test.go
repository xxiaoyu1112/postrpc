package test

import (
	"testing"
)

func TestPredict(t *testing.T) {
	// ctx, cancel := context.WithDeadline(context.Background(), time.Now().Add(time.Duration(5*time.Second)))
	// defer cancel()
	// rpc.Predict(ctx, "gd2", "1.0")
}
